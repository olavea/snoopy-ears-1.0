// Always nice to have a very simple function to check
// if it's your set up or your time-travel function that is not working.
// http://localhost:8000/api/hello

export default function handler(req, res) {
  res.send("👋🏻");
}
