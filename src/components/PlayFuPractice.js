import { Link } from "gatsby"
import React from "react"

export default function Layout() {
    return (
        <div>
          <header>
            <nav>
              <Link to="/">🎩 is Home</Link>
            </nav>
          </header>
        </div>
    )
};